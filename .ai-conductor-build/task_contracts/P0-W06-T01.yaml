# Task Contract — P0-W06-T01

```yaml
contract_version: 1
id: P0-W06-T01
phase: P0
status: IMPLEMENTED

authority:
  blueprint: "2.4"
  protocol: "1.0"
  phase_manifest: "1.0"

section_refs:
  - "Blueprint §22"
  - "Build Protocol §28"
  - "Phase Manifest P0-C06"

objective: >
  Define the backend skill contract — the seven-field skill definition, initial
  skill set, SkillResult artifact, and prompt-vs-enforcement rule — before
  capability-table (AC-07) and Skill Runtime work.

allowed_files:
  - ".ai-conductor-build/architecture_changes/AC-06-skill-contract.md"
  - ".ai-conductor-build/task_contracts/P0-W06-T01.yaml"
  - ".ai-conductor-build/verification_reports/P0-W06-T01.md"
  - ".ai-conductor-build/BUILD_STATE.json"

forbidden_files:
  - "PROJECT/**"
  - "BLUEPRINT/**"
  - "GOVERNANCE/**"
  - "FAILURE_MEMORY/**"
  - "KNOWLEDGE/**"
  - "48 HOURS/**"

forbidden_actions:
  - "any production code"
  - "editing governing documents"

dependencies:
  - "P0-W05-T01 accepted (construction order §30)"

preconditions:
  - "Phase 0 active."

inputs:
  - "Blueprint §22; Build Protocol §28; Knowledge Base §35–§39."

outputs:
  - "AC-06 architecture contract."

implementation_requirements:
  - "Seven mandatory definition fields per Build Protocol §28."
  - "Initial six-skill backend set exactly as listed."
  - "Skill permissions enforced at capability boundary, never by prompt."
  - "SkillResult structured artifact preserved as evidence."

invariants_preserved: ["7","8","10"]
invariants_proven: []

required_tests:
  - "Level 1 deterministic: contract self-checklist."

acceptance:
  - "Contract consistent with Blueprint §22, Build Protocol §28, KB §35–§39."

non_goals:
  - "Workflow stepper UI; Skill Runtime mechanics; gstack installation (U-0002)."

risk_level: low

done_when:
  - "Verification report records skill-contract definitions present and consistent."
```
