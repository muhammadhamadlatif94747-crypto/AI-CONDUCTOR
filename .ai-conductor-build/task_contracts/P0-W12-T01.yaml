# Task Contract — P0-W12-T01

```yaml
contract_version: 1
id: P0-W12-T01
phase: P0
status: IMPLEMENTED

authority:
  blueprint: "2.4"
  protocol: "1.0"
  phase_manifest: "1.0"

section_refs:
  - "Blueprint §10.2"
  - "Verification Gates §7, §13"
  - "Phase Manifest P0-C12"

objective: >
  Define the Clock abstraction with the hard rule that no reliability-core file
  calls the real system clock directly — enabling deterministic virtual-time tests.

allowed_files:
  - ".ai-conductor-build/architecture_changes/AC-12-clock-trait.md"
  - ".ai-conductor-build/task_contracts/P0-W12-T01.yaml"
  - ".ai-conductor-build/verification_reports/P0-W12-T01.md"
  - ".ai-conductor-build/BUILD_STATE.json"

forbidden_files:
  - "PROJECT/**"
  - "BLUEPRINT/**"
  - "GOVERNANCE/**"
  - "FAILURE_MEMORY/**"
  - "KNOWLEDGE/**"
  - "48 HOURS/**"

forbidden_actions:
  - "any production code"
  - "editing governing documents"

dependencies:
  - "P0-W11-T01 accepted (construction order §30)"

preconditions:
  - "Phase 0 active."

inputs:
  - "Blueprint §10.2 hard architecture rule; Verification Gates clock_source."

outputs:
  - "AC-12 architecture contract."

implementation_requirements:
  - "Injectable Clock trait; RealClock production / VirtualClock tests."
  - "Direct system-clock reads forbidden in core; gate enforcement noted."
  - "clock_source recorded on timestamp-bearing records."

invariants_preserved: ["5","8"]
invariants_proven: []

required_tests:
  - "Level 1 deterministic: contract self-checklist."

acceptance:
  - "Contract consistent with Blueprint §10.2 and Verification Gates §7/§13."

non_goals:
  - "Timer mechanics; FakeProvider matrix; UI."

risk_level: low

done_when:
  - "Verification report records the Clock trait + hard rule present."
```
