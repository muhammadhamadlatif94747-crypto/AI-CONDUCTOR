# Task Contract — P0-W10-T01

```yaml
contract_version: 1
id: P0-W10-T01
phase: P0
status: IMPLEMENTED

authority:
  blueprint: "2.4"
  protocol: "1.0"
  phase_manifest: "1.0"

section_refs:
  - "Blueprint §4.2.1"
  - "Build Protocol §13"
  - "Phase Manifest P0-C10"

objective: >
  Define Mission and Step state machines — independent of Attempt — with explicit
  enums, transition tables, and transition authority.

allowed_files:
  - ".ai-conductor-build/architecture_changes/AC-10-mission-step-state-machines.md"
  - ".ai-conductor-build/task_contracts/P0-W10-T01.yaml"
  - ".ai-conductor-build/verification_reports/P0-W10-T01.md"
  - ".ai-conductor-build/BUILD_STATE.json"

forbidden_files:
  - "PROJECT/**"
  - "BLUEPRINT/**"
  - "GOVERNANCE/**"
  - "FAILURE_MEMORY/**"
  - "KNOWLEDGE/**"
  - "48 HOURS/**"

forbidden_actions:
  - "any production code"
  - "editing governing documents"

dependencies:
  - "P0-W09-T01 accepted (construction order §30)"
  - "AC-01 accepted (attempt machine independence boundary)"
  - "AC-08 accepted (mission-level acceptance evaluation)"

preconditions:
  - "Phase 0 active."

inputs:
  - "Blueprint §4.2.1 enums, tables, authority paragraph."

outputs:
  - "AC-10 architecture contract."

implementation_requirements:
  - "Enums and transitions exactly per Blueprint §4.2.1."
  - "Single-executor mutation authority; every transition an event."
  - "Mission Succeeded gated on mission-scoped acceptance re-evaluation."
  - "Skipped only by explicit contract declaration."

invariants_preserved: ["1","2","5","8"]
invariants_proven: []

required_tests:
  - "Level 1 deterministic: contract self-checklist."

acceptance:
  - "Machines defined independently of Attempt; consistent with AC-01/AC-04/AC-08."

non_goals:
  - "Attempt lifecycle; model-checking method; planning algorithms; UI."

risk_level: low

done_when:
  - "Verification report records both machines + tables defined independently of Attempt."
```
