# Task Contract — P0-W13-T01

```yaml
contract_version: 1
id: P0-W13-T01
phase: P0
status: IMPLEMENTED

authority:
  blueprint: "2.4"
  protocol: "1.0"
  phase_manifest: "1.0"

section_refs:
  - "Phase Manifest §4.6 (P0 gate: task contracts for Phase 1 are ready), §5.3"
  - "Task Contracts §30"
  - "Build Protocol §9"

objective: >
  Prepare complete, bounded task contracts for all eleven Phase 1 work packages
  so P1 execution can begin without contract invention during implementation.

allowed_files:
  - ".ai-conductor-build/task_contracts/P1-prepared-task-contracts.yaml"
  - ".ai-conductor-build/task_contracts/P0-W13-T01.yaml"
  - ".ai-conductor-build/verification_reports/P0-W13-T01.md"
  - ".ai-conductor-build/BUILD_STATE.json"

forbidden_files:
  - "PROJECT/**"
  - "BLUEPRINT/**"
  - "GOVERNANCE/**"
  - "FAILURE_MEMORY/**"
  - "KNOWLEDGE/**"
  - "48 HOURS/**"

forbidden_actions:
  - "any production code"
  - "editing governing documents"
  - "starting any Phase 1 implementation"

dependencies:
  - "P0-W12-T01 accepted (construction order §30)"
  - "AC-01..AC-12 defined (contracts referenced by P1 packages)"

preconditions:
  - "Phase 0 active; all architecture contracts W01–W12 accepted."

inputs:
  - "Phase Manifest §5.3 work packages + §5.5 gate; Blueprint §23 Phase 1 checklist."

outputs:
  - "P1-prepared-task-contracts.yaml covering P1-W01..P1-W11."

implementation_requirements:
  - "Every package: title, section_refs, allowed_files, done_when with testable proofs."
  - "F-0001 single-writer rule embedded in P1-W05 done_when."
  - "AC-11 store/classification separation embedded in P1-W07."

invariants_preserved: ["9","15"]
invariants_proven: []

required_tests:
  - "Level 1 deterministic: coverage check — 11/11 manifest packages present."

acceptance:
  - "Coverage complete vs Phase Manifest §5.3; no package invented beyond the manifest;
     no Phase 1 code written."

non_goals:
  - "Any implementation; verification-gate preparation (P0-W14); UI."

risk_level: low

done_when:
  - "Verification report records 11/11 coverage and manifest fidelity."
```
