# Task Contract — P0-W07-T01

```yaml
contract_version: 1
id: P0-W07-T01
phase: P0
status: IMPLEMENTED

authority:
  blueprint: "2.4"
  protocol: "1.0"
  phase_manifest: "1.0"

section_refs:
  - "Blueprint §16, §7.1, module map"
  - "Build Protocol §28"
  - "Phase Manifest P0-C07"

objective: >
  Define the capability table + tool boundary contract: capability identities,
  skill/tool permissions, enforcement boundary, and tool-execution-boundary
  location — without inventing capabilities the Blueprint does not define.

allowed_files:
  - ".ai-conductor-build/architecture_changes/AC-07-capability-table-tool-boundary.md"
  - ".ai-conductor-build/task_contracts/P0-W07-T01.yaml"
  - ".ai-conductor-build/verification_reports/P0-W07-T01.md"
  - ".ai-conductor-build/BUILD_STATE.json"

forbidden_files:
  - "PROJECT/**"
  - "BLUEPRINT/**"
  - "GOVERNANCE/**"
  - "FAILURE_MEMORY/**"
  - "KNOWLEDGE/**"
  - "48 HOURS/**"

forbidden_actions:
  - "any production code"
  - "editing governing documents"

dependencies:
  - "P0-W06-T01 accepted (skill set being permissioned)"

preconditions:
  - "Phase 0 active."

inputs:
  - "Blueprint §16 (four identities, boundary hard rule), §7.1 (sandbox), module map."

outputs:
  - "AC-07 architecture contract."

implementation_requirements:
  - "Capability set limited to Blueprint-defined identities; extension requires cited architecture change."
  - "Enforcement at tool-execution boundary; no alternate execution route."
  - "Boundary location identified (tool_boundary.rs gate + §7.1 sandbox)."

invariants_preserved: ["8","10","12"]
invariants_proven: []

required_tests:
  - "Level 1 deterministic: contract self-checklist."

acceptance:
  - "All four P0-C07 elements present; no invented capabilities; consistent with AC-06."

non_goals:
  - "Provider Capability Registry (§10.3); verification pipeline logic; UI."

risk_level: low

done_when:
  - "Verification report records all four required elements present."
```
