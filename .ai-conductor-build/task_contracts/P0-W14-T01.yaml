# Task Contract — P0-W14-T01

```yaml
contract_version: 1
id: P0-W14-T01
phase: P0
status: IMPLEMENTED

authority:
  blueprint: "2.4"
  protocol: "1.0"
  phase_manifest: "1.0"

section_refs:
  - "Phase Manifest §4.6 (gates for Phase 1 ready), §5.5 (P1 gate)"
  - "Task Contracts §30"
  - "Verification Gates §7–§9, §13"

objective: >
  Prepare binding verification gates for all Phase 1 work packages and the
  phase gate, so P1 proofs are fixed before implementation begins.

allowed_files:
  - ".ai-conductor-build/verification_gates/P1-prepared-verification-gates.yaml"
  - ".ai-conductor-build/task_contracts/P0-W14-T01.yaml"
  - ".ai-conductor-build/verification_reports/P0-W14-T01.md"
  - ".ai-conductor-build/BUILD_STATE.json"

forbidden_files:
  - "PROJECT/**"
  - "BLUEPRINT/**"
  - "GOVERNANCE/**"
  - "FAILURE_MEMORY/**"
  - "KNOWLEDGE/**"
  - "48 HOURS/**"

forbidden_actions:
  - "any production code"
  - "editing governing documents"
  - "starting any Phase 1 implementation"

dependencies:
  - "P0-W13-T01 accepted (gate targets = prepared contracts)"

preconditions:
  - "Phase 0 active."

inputs:
  - "Phase Manifest §5.5 required-proof list; Verification Gates trust classes."

outputs:
  - "P1-prepared-verification-gates.yaml covering VG-P1-W01..W11 + phase gate."

implementation_requirements:
  - "Every manifest §5.5 proof class mapped to a concrete gate."
  - "Fail-closed rule stated; F-0001 forever-test included."
  - "Phase gate requires architecture audit (no State Engine bypass)."

invariants_preserved: ["1","5","15"]
invariants_proven: []

required_tests:
  - "Level 1 deterministic: coverage + manifest-fidelity check."

acceptance:
  - "Gates consistent with prepared contracts and Phase Manifest §5.5; no code written."

non_goals:
  - "Gate execution; any implementation; later-phase gates."

risk_level: low

done_when:
  - "Verification report records full coverage of §5.5 proof classes."
```
