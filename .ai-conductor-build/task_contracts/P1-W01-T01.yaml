# Task Contract — P1-W01-T01

```yaml
contract_version: 1
id: P1-W01-T01
phase: P1
status: IMPLEMENTED

authority:
  blueprint: "2.4"
  protocol: "1.0"
  phase_manifest: "1.0"

section_refs:
  - "Blueprint §4.2, §4.2.1"
  - "AC-01, AC-10"
  - "Phase Manifest P1-W01"
  - ".ai-conductor-build/task_contracts/P1-prepared-task-contracts.yaml (P1-W01-T01)"

objective: >
  Implement the State Engine with enforced transition tables for AttemptStatus,
  MissionStatus, and StepStatus — the first production code, kept I/O-free.

allowed_files:
  - "PROJECT/Cargo.toml"
  - "PROJECT/crates/conductor-kernel/**"
  - ".ai-conductor-build/task_contracts/P1-W01-T01.yaml"
  - ".ai-conductor-build/verification_reports/P1-W01-T01.md"
  - ".ai-conductor-build/BUILD_STATE.json"
  - ".gitignore"

forbidden_files:
  - "BLUEPRINT/**"
  - "GOVERNANCE/**"
  - "FAILURE_MEMORY/**"
  - "KNOWLEDGE/**"
  - "48 HOURS/**"

forbidden_actions:
  - "editing governing documents"
  - "any I/O or clock reads in conductor-kernel"
  - "integrating Aider/providers (P1 non-goal)"

dependencies:
  - "Phase 0 accepted; AC-01 + AC-10 defined"

preconditions:
  - "Phase 1 active."

inputs:
  - "AC-01 §2 structural table (7 edges) + §5 authorization table."
  - "AC-10 §3 tables (11 mission + 6 step edges)."

outputs:
  - "conductor-kernel::state with three enums, can_transition_to predicates,"
    "try_attempt_transition enforcing structure + actor authorization."

implementation_requirements:
  - "Exhaustive pair tests over every ordered enum pair for all three machines."
  - "Succeeded edges accepted ONLY from TransitionActor::VerificationEngine (Invariant 14)."
  - "Terminal states accept no outgoing edges."
  - "Specific typed errors (IllegalEdge vs UnauthorizedActor)."

invariants_preserved: ["1","2","5","8","11","14"]
invariants_proven: ["14"]

required_tests:
  - "cargo test: 15 deterministic unit tests incl. exhaustive table checks."

acceptance:
  - "All four VG-P1-W01-T01 proofs evidenced in verification report."

non_goals:
  - "Event log emission (P1-W03); CancellationOutcome enum (P1-W02); persistence."

risk_level: low

done_when:
  - "Legal transitions accepted; illegal rejected; tests exercise invalid transitions;
     semantics match AC-01/AC-10 — all verified by passing cargo test."
```
